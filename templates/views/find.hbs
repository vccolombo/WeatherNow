<!DOCTYPE html>
<html>

<head>
    <title>Weather</title>
    <link rel="icon" href="/img/weather.png">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="main-content">
        {{>header}}
        {{>searchBar}}

        <ul>
            {{#each cities}}
            <div><a href="/weather?latitude={{this.center.[1]}}&longitude={{this.center.[0]}}">{{ this.place_name }}</a></div>
            {{/each}}
        </ul>


    </div>

    {{>footer}}

    <script src="/js/app.js"></script>
</body>

</html>